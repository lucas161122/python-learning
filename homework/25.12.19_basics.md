

# Python Basics – 2025.12.19

## Part A – Quiz Questions (Concepts)

### Q1. Data types

Write the Python **data type** for each value (choose from: `bool`, `str`, `int`, `float`):

1. `True` → _______
2. `"hello"` → _______
3. `0` → _______
4. `3.14` → _______

---

### Q2. Good variable names

Which is the **best** variable name according to these rules:

* all lowercase
* uses underscores
* meaningful name

a) `x`
b) `UserName`
c) `user_name`

Answer: _______

Explain in one short sentence **why**:

> ---

---

### Q3. Triple-quoted strings

Which of these is a **correct** triple-quoted string in Python?

a) `story = """Once upon a time"`
b) `story = """Once upon a time"""`
c) `story = "Once upon a time"""`

Answer: _______

Then write **one**
triple-quoted string yourself that spans **two lines**:

```python
story = """________________
________________"""
```

---

### Q4. Indexing and slicing

Given:

```python
text = "Python"
```

Fill in the results:

1. `text[0]` → _______
2. `text[-1]` → _______
3. `text[1:4]` → _______
4. `text[:3]` → _______

---

### Q5. Escape characters and new line

You want to print exactly:

```text
He said: "I love Python!"
Let's learn together.
```

Which of these strings is correct?

a) `"He said: "I love Python!"\nLet's learn together."`
b) `"He said: \"I love Python!\"\nLet's learn together."`
c) `"He said: \"I love Python!\" Let's learn together."`

Answer: _______

---

### Q6. f-strings

You have:

```python
name = "Lucas"
age = 9
```

Write **one** f-string that prints:

```text
Lucas is 9 years old.
```

```python
print(_________________________________)
```

---

### Q7. String methods

Let:

```python
s = "  hello lucas  "
```

Which method would you use to:

1. Remove the spaces on both sides? → `.____________()`
2. Turn it into all uppercase letters? → `.____________()`
3. Turn it into title case (`"Hello Lucas"`) while keeping spaces? → `.____________()`

---

### Q8. `in` and `not in`

Let:

```python
message = "I love Python"
```

Are these expressions `True` or `False`?

1. `"Python" in message` → _______
2. `"python" in message` → _______
3. `"Java" not in message` → _______

---

### Q9. Operators `/`, `//`, `%`, `**`, `+=`

Without running Python, compute the results:

```python
a = 9 / 2
b = 9 // 2
c = 9 % 2
d = 2 ** 4
x = 5
x += 3
```

Fill in:

1. `a =` _______
2. `b =` _______
3. `c =` _______
4. `d =` _______
5. `x =` _______

---

### Q10. `round`, `abs`, and `math`

What do these return?

1. `round(3.6789, 2)` → _______
2. `abs(-15)` → _______

If we write:

```python
import math
```

what does:

3. `math.sqrt(49)` → _______
4. `round(math.pi, 2)` → _______

---

### Q11. Falsy values

Which of these values are **falsy** in Python? (Circle or list them)

* `0`
* `1`
* `""`
* `"hello"`
* `None`
* `[]`

---

### Q12. Comparison operators

Decide `True` or `False`:

1. `3 < 5` → _______
2. `10 == 10` → _______
3. `7 != 7` → _______
4. `8 >= 2 * 4` → _______

---

### Q13. Boolean logic: `and`, `or`, `not`

Let:

```python
A = True
B = False
C = True
```

Evaluate:

1. `A and B and not C` → _______
2. `A or B or not C` → _______

---

### Q14. If/else vs one-line (ternary)

We have:

```python
if score >= 60:
    result = "pass"
else:
    result = "fail"
```

1. Rewrite this as **one line** using the ternary operator:

```python
result = ____________________________________________
```

2. In that line:

   * the **condition** is: _____________________
   * the result if True: _____________________
   * the result if False: _____________________

---

### Q15. Functions: return value `None`

Look at this code:

```python
def greet(name):
    print(f"Hello, {name}!")

result = greet("Lucas")
print(result)
```

1. What does the **first** `print` show?

   > ---

2. What does the **second** `print(result)` show?

   > ---

3. Why? (Short answer)

   > ---

---

## Part B – Programming Tasks (Write Code)

For each task, **write a Python program** that produces the described behavior and output.
You may put each task in a separate file or all in one file with comments like `# Task 1`.

---

### P1. Data types & `type()`

Write a program that:

1. Creates 4 variables: a boolean, a string, an integer, and a float.
2. Prints each variable and its type using `type(...)`.

Example output (your values can be different):

```text
True <class 'bool'>
Lucas <class 'str'>
9 <class 'int'>
3.5 <class 'float'>
```

---

### P2. Triple-quoted story and slicing

Create a triple-quoted string called `story` that has **at least 2 lines** of text.

Then:

1. Print the whole story.
2. Print the **first character**.
3. Print the **last character**.
4. Print the **first 10 characters** using slicing.

---

### P3. Escape characters and new lines

Write a program that prints exactly this using **one** `print`:

```text
He said: "Python is cool!"
Let's learn it together.
```

You must use `\n` and escaped quotes `\"` inside the string.

---

### P4. f-string introduction

Create variables `name` and `age`.
Use an f-string to print:

```text
Hi, my name is Lucas and I am 9 years old.
```

(You may change the values, but keep the same structure.)

---

### P5. Clean and format a name

Ask the user to input their name, but they might accidentally add spaces, like:

```text
"   lucas   "
```

Your program should:

1. Use `.strip()` to remove spaces on both sides.
2. Use `.title()` to format it nicely (e.g. `"Lucas"`).
3. Print:

```text
Hello, Lucas!
```

(using the cleaned-up name).

---

### P6. Find and replace in a sentence

Create a variable:

```python
sentence = "I like Java and Java is fun."
```

Write a program that:

1. Uses `.find()` to get the position of the **first** `"Java"`.
2. Creates a new string where **all** `"Java"` are replaced with `"Python"`.
3. Prints something like:

```text
First 'Java' found at position: 7
New sentence: I like Python and Python is fun.
```

---

### P7. Check if a word is in a sentence

Ask the user to input:

1. A sentence
2. A word

Print:

* `"Found!"` if the word is in the sentence (`in`)
* `"Not found!"` otherwise

Also print one line using `not in`, for example:

```text
The word is not in the sentence: True/False
```

---

### P8. Number operations with `/`, `//`, `%`, `**`, `+=`

Ask the user to enter an integer, e.g. `7`.

Print the following:

```text
n / 2 = ...
n // 2 = ...
n % 2 = ...
n ** 2 = ...
n after += 5 is ...
```

Use the actual operators: `/`, `//`, `%`, `**`, and `+=`.

---

### P9. `round`, `abs`, and circle area (`math` module)

Write a program that:

1. Asks the user for a decimal number (e.g. `-3.5678`) and prints:

   * its absolute value using `abs`
   * it rounded to 2 decimal places using `round(x, 2)`

2. Imports `math`.

3. Asks for a circle radius (float).

4. Calculates the area using `math.pi * r * r`.

5. Prints the area rounded to 2 decimals, e.g.:

```text
Absolute value: 3.5678
Rounded to 2 decimals: 3.57
Area of the circle: 12.57
```

---

### P10. Age calculator with comments and type conversion

Write a program with **at least 2 comments** explaining what the code does.

The program should:

1. Ask the user for their **birth year** as input (string).
2. Convert it to an integer.
3. Compute their age in the year 2025.
4. Print using an f-string:

```text
You are X years old.
```

---

### P11. Falsy vs truthy checker

Create a list:

```python
values = [0, "", None, 5, "hello"]
```

Loop over this list. For each value `v`:

* If `v` is truthy, print: `Truthy: <value>`
* If `v` is falsy, print: `Falsy: <value>`

Use:

```python
if v:
    ...
else:
    ...
```

---

### P12. First vowel finder with `break`

Ask the user to type a word (e.g. `sky`, `apple`).

Use a `for` loop to go through each character:

1. When you find the **first vowel** (`a, e, i, o, u`, either lowercase or uppercase), print:

   ```text
   First vowel: a
   ```

   and then `break` the loop.

2. If there is **no vowel** in the word, print:

   ```text
   No vowel found.
   ```

(Hint: you can use a flag variable like `found = False`.)

---

### P13. Nested `for` loops – 3×3 multiplication table

Print a 3×3 multiplication table using nested loops.

Expected output:

```text
1 2 3
2 4 6
3 6 9
```

Hints:

* Outer loop: numbers 1 to 3
* Inner loop: multiply the row number by 1, 2, 3
* Use `end=" "` in `print` to stay on the same line, and a separate `print()` to go to the next line.

---

### P14. Iterables: `range`, `str`, and `list`

Write a program that:

1. Creates:

   ```python
   r = range(3)
   s = "Hi!"
   lst = [10, 20, 30]
   ```
2. Prints the **type** of each (using `type`).
3. Uses three `for` loops to print all elements of each one.

Example:

```text
range:
0
1
2
string:
H
i
!
list:
10
20
30
```

---

### P15. Password check with infinite `while` loop and `break`

Write a program that keeps asking for a password until the user gets it right.

Requirements:

* Store the correct password in a variable, e.g. `correct_password = "python123"`.
* Use `while True:`.
* If the input matches the correct password:

  * print `"Welcome!"`
  * `break` the loop
* Otherwise print `"Try again."` and ask again.

---

### P16. Sum numbers until total > 100

Use a `while True` loop.

* Start with `total = 0`.
* Each loop, ask the user to input a number (int) and add it to `total`.
* If `total` becomes **greater than 100**, print:

```text
Total is now X, stopping.
```

and `break`.

---

### P17. Score grader (if/elif/else and comparisons)

Ask the user to input a score between 0 and 100.

Print:

* `"Perfect!"` if the score is exactly 100
* `"Good job!"` if the score is **60 or above but less than 100**
* `"Try again!"` if the score is **less than 60**

Use comparison operators (`==`, `<`, `>=`) and `if / elif / else`.

---

### P18. Roller coaster logic with `and`, `or`, `not`

Ask the user:

1. `"Do you have a ticket? (yes/no): "`
2. `"Are you with an adult? (yes/no): "`
3. `"Are you scared? (yes/no): "`

Convert each answer to booleans: `True` or `False`.

Rules:

* You **can ride** if you have a ticket AND are with an adult AND are **not** scared.
* Otherwise, you **cannot ride**.

Print either:

* `"You can ride!"`
* or `"You cannot ride."`

Use a boolean expression with `and` and `not`.

---

### P19. Ternary operator for temperature

Ask the user to input a temperature (integer).

* If temperature > 25 → feeling is `"hot"`
* Otherwise → feeling is `"cold"`

First write it with a normal `if/else`, then write it again using the **ternary operator**:

```python
feeling = "hot" if temperature > 25 else "cold"
```

Print the value of `feeling` in both cases.

---

### P20. Function that returns `None` vs returns a value

Write two functions:

1. `shout(message)` – prints the message in uppercase, but does **not** return anything.
2. `to_upper(message)` – returns the uppercase version of the message.

Then:

```python
result1 = shout("hello")
result2 = to_upper("hello")
print("result1:", result1)
print("result2:", result2)
```

Observe what is printed, and add a **comment** explaining why `result1` is `None` but `result2` is not.

---

### P21. Function with required and optional parameters

Write a function:

```python
def greet(name, greeting="Hello"):
    ...
```

It should print, for example:

```text
Hello, Lucas!
```

or

```text
Good morning, Lucas!
```

Call this function at least 3 times:

1. With only `name`
2. With both `name` and `greeting`
3. Using named arguments: `greet(name="Lucas", greeting="Hi")`

---

### P22. `*args` – add many numbers

Write a function:

```python
def add_numbers(*args):
    ...
```

Inside, loop over `args`, add all the numbers, and **return** the total.

Then call:

```python
print(add_numbers(1, 2, 3))
print(add_numbers(10, 20, 30, 40))
```

Expected kind of output:

```text
6
100
```

---

### P23. `**kwargs` – describe a pet

Write a function:

```python
def describe_pet(**kwargs):
    ...
```

Inside, get `name` and `animal_type` from `kwargs` and print a sentence like:

```text
This is my cat named Kitty.
```

Call it like:

```python
describe_pet(name="Kitty", animal_type="cat")
describe_pet(name="Rex", animal_type="dog")
```

---

### P24. Simple profile printer with `**kwargs`

Write a function:

```python
def show_profile(**kwargs):
    ...
```

This function should loop through `kwargs` and print each key-value pair, like:

```text
name: Lucas
age: 9
favorite_game: Minecraft
```

Call it at least twice with different keyword arguments.

---

### P25. Use `*args` with a list

Reuse your `add_numbers(*args)` function from P22.

Write a program that:

1. Asks the user to enter 3 integers.
2. Stores them in a list, e.g. `nums = [a, b, c]`.
3. Calls `add_numbers(*nums)` and prints the result.

---

### P26. Mini “Amusement Park” program

Combine what you learned:

1. Ask for the user’s name and age.
2. Convert age to `int`.
3. Ask `"Are you brave? (yes/no): "`
4. Use a boolean expression with `and` / `not` to decide if they can go on a **scary ride**:

   * must be at least 10 years old
   * must answer `"yes"` to brave
5. Use an f-string to print either:

   * `"Lucas, you can go on the scary ride!"`
   * or `"Lucas, maybe try a smaller ride."`

Add **at least two comments** explaining what your code does.

---

### P27. Type and value inspector

Write a function:

```python
def inspect_value(value):
    ...
```

It should print something like:

```text
Value: hello
Type: <class 'str'>
Is truthy? True
```

Test it with different inputs:

* `inspect_value(0)`
* `inspect_value("")`
* `inspect_value("hello")`
* `inspect_value(42)`

---

### P28. Times table with nested loops and f-strings

Write a program that asks the user for a number `n` and prints the `n` times table from 1 to 10 using nested loops or one loop and f-strings.

Example (if `n = 3`):

```text
3 x 1 = 3
3 x 2 = 6
...
3 x 10 = 30
```

---

### P29. Counting characters in a string

Ask the user for a string.

Use a `for` loop to count how many times each character appears, and print results like:

```text
Character 'a' appears 2 times
Character 'b' appears 1 times
...
```

(For Lucas, you can start with just counting a few chosen letters like vowels if full dict is too much.)

---

### P30. Bonus: combine `*args` and `**kwargs`

Write a function:

```python
def debug_print(*args, **kwargs):
    ...
```

It should:

1. Print all `args` in one line:

   ```text
   args: (value1, value2, ...)
   ```
2. Print all `kwargs` in one line:

   ```text
   kwargs: {'key1': value1, 'key2': value2, ...}
   ```

Then call it like:

```python
debug_print(1, 2, 3, name="Lucas", age=9)
```

